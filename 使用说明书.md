# ColorGrabber 取色测浓度 · 使用说明书

**版本：v1.0**　|　适用安卓 7.0（API 24）及以上

---

## 一、这个 App 是做什么的

用手机摄像头对显色后的试管溶液**取色**，把颜色量化为 **RGB / HSV / Lab / 三通道吸光度**，用于光催化降解四环素实验的比色法分析。

> 原理：显色剂与四环素发生显色反应，颜色深浅与浓度成正比；吸光度（A）与浓度呈线性关系，可据此反推浓度。
> 本版本完成「取色与吸光度读取」；由吸光度拟合浓度标准曲线为后续版本功能。

---

## 二、安装

1. 把 `ColorGrabber-v1.0.apk` 传到安卓手机。
2. 点击安装；若提示「未知来源 / 风险」，选择**继续安装 / 允许**。
3. 首次进入会请求**相机权限**，点**允许**。

---

## 三、拍摄条件（务必固定，保证数据可比）

- 试管置于**白色背景**前；
- 使用**稳定补光**，避免忽明忽暗、避免阳光直射；
- 尽量保持**同一距离、同一角度、同一光照**拍每一批样品；
- 避免反光点、气泡进入取色区域。

> 同一条件下拍摄，不同样品的颜色/吸光度才有可比性。

---

## 四、界面总览

- **取景页（首页）**：实时预览 + 中间黄色取色框；右上角 🕘 历史、⋮ 更多（锁定曝光）。
- **测量页**：拍照或选图后进入，可缩放/平移图片、拖动取色框，在静止画面上从容取色。
- **历史页**：所有记录列表，点任一条可重新打开测量；右上角可导出 CSV。

每个画面顶部都有**固定的 RGB 读数条**（色块 + RGB 值 + 十六进制），下面依次是 HSV、Lab、吸光度、白平衡。

---

## 五、三种取色方式

| 方式 | 操作 | 适用 |
|---|---|---|
| 实时取色 | 在取景页对准试管，读中间取色框的实时数值，点**记录** | 快速读数 |
| 拍照取色 | 取景页点 **📷 拍摄** → 进入测量页慢慢选区 | 推荐，最稳 |
| 相册导入 | 取景页点 **🖼 从相册选图** → 选已有照片 → 测量页取色 | 复用已拍照片 |

### 推荐标准流程（拍照取色）

1. 取景页对准试管，构图稳定后点 **📷 拍摄**。
2. 进入测量页，**双指缩放 / 单指拖动**调整图片；**拖动取色框或拉四角**框住试管溶液区。
3. 把取色框移到**白色背景**上，点 **点白校准**（校正补光偏色）。
4. 若要测吸光度：把框移到**空白参比**（零浓度/纯背景）上，点 **设参比**。
5. 把框移回**待测溶液**，读出 RGB / HSV / Lab / 吸光度。
6. 点 **记录此点**，填样品名/备注/降解时间点，保存。

---

## 六、关键操作详解

- **取色框**：读数是框内**所有像素的平均颜色**。框越大越平滑，但别框到管壁、气泡、反光点。
- **点白校准（白平衡）**：对准白背景点一下，消除补光的颜色偏色，让颜色还原更准。
- **设参比 / 吸光度**：吸光度 A = −log₁₀(I/I₀)，**I₀ 是空白参比的亮度基线**。先对空白「设参比」，之后样品才会显示三通道吸光度（R/G/B）；选**变化最敏感的通道**与浓度建关系。没设参比时显示「未标定」。
- **白平衡微调（折叠）**：点「⚙ 白平衡微调」展开，色温/R/G/B 增益滑块可手动微调；在测量页拖动时**整张照片会随之实时变色**，所见即所测。
- **⚠ 过曝提示**：取色区过亮（饱和）会提示过曝，此时数据不准，请调暗补光或降低曝光（可用「锁定曝光」稳定亮度）。

---

## 七、记录、历史与导出

- **记录**：保存当前读数；拍照/选图的记录会连同图片存入手机相册「**ColorGrabber**」相册。
- **历史**：右上角 🕘 进入；**点任一条记录可重新打开该图片继续测量**（自动还原取色框与增益）。
- **导出 CSV**：历史页右上角 ⋮ →「导出 CSV」，含全部字段（时间、样品名、RGB、HSV、Lab、吸光度、增益等），可用 Excel 直接打开（中文不乱码）。

---

## 八、注意事项

- 务必**固定拍摄条件**，否则不同批次数据不可比。
- 每批测量建议先**点白校准**、再**设参比**、最后测样品。
- 实时记录（不拍照）不保存图片，历史里无法再打开该图。
- 取色框尽量只覆盖均匀的溶液区域。

---

## 九、版本信息

- 名称：ColorGrabber　版本：v1.0
- 本版功能：取色（RGB/HSV/Lab/三通道吸光度）、手动白平衡、实时/拍照/相册三种取色、历史与 CSV 导出、专属相册。
- 后续计划：颜色→浓度标准曲线标定。
